cmake_minimum_required(VERSION 3.12)

project(Example4 VERSION 0.0.3)

# Let it be sumLib "external" library:
#add_subdirectory(sumLib)
#
#use cmake .. -DCMAKE_PREFIX_PATH=/path for customizing prefix path
add_subdirectory(src)