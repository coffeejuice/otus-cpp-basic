#pragma once

#include <iostream>

#include <algorithm> // std::shuffle
#include <chrono> 	 // std::chrono::system_clock
#include <random>	 // std::default_random_engine

#include <vector>


void make_random_shuffle(std::vector<int>& values);

